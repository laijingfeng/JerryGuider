//This code was generated by a tool at 2017-07-17 05:15:41

import "common_guider.proto";

package Table;

message Guider
{
	optional uint32 id = 1; //ID
	optional uint32 next_id = 2; //下一个引导
	optional bool is_server = 3; //是否服务器记录做重现
	optional uint32 guiderUI_id = 4; //第一个UI引导
	optional string ui_camera = 5; //UI相机
}

message Guider_ARRAY
{
	repeated Guider rows = 1;
}

message GuiderUI
{
	optional uint32 id = 1; //ID
	optional uint32 next_id = 2; //下一个引导
	optional bool statistical = 3; //做统计
	optional bool send_finish = 4; //做完这步可认为结束
	optional string ui_camera = 5; //UI相机出现变更
	optional string node_path = 6; //路径
	optional Common.GuiderAnimType ui_anim_type = 7 [default = ANIM_TYPE_NONE]; //提示动画类型
	optional string attach_ui = 8; //附加UI动画
	optional Common.Vec3 follow_offset = 9; //UI动画偏移
	optional Common.GuiderFollowType follow_type = 10 [default = FOLLOW_ONCE]; //UI动画刷新频率
	optional Common.GuiderTriggerType begin_type = 11 [default = TRIGGER_INVALID]; //开始方式
	optional Common.GuiderTriggerType end_type = 12 [default = TRIGGER_INVALID]; //结束方式
	optional bool need_mask = 13; //是否需要遮罩
	optional string tip_text = 14; //提示内容
	optional Common.Vec3 tip_pos = 15; //提示位置
	optional bool use_replacement = 16; //使用替代物体
	optional Common.Vec3 replacement_size = 17; //替代物大小
	optional Common.Vec3 replacement_offset = 18; //替代物偏移
	optional Common.GuiderEvent begin_event = 19; //开始事件
	optional Common.GuiderEvent end_event = 20; //结束事件
	optional bool is_3d = 21; //是否是3D
	optional string camera_3d = 22; //3D相机
}

message GuiderUI_ARRAY
{
	repeated GuiderUI rows = 1;
}

message GuiderMsg
{
	optional uint32 id = 1; //ID
	repeated uint32 guider_id_list = 2; //引导id列表
	optional Common.GuiderMsgCmd cmd = 3 [default = MSG_CMD_INVALID]; //指令
}

message GuiderMsg_ARRAY
{
	repeated GuiderMsg rows = 1;
}

